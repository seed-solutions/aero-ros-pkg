(defun testsr ()
  (load "/home/wesleyc/docs/catkin_ws/src/robot_model_compiler/euslisp/samplerobot-interface.l")
  (samplerobot-init)
  (pprint (send *sr* :slots))
  (print "")
  (print "")
  (print "")
  (send *sr* :torso)
)

(defun testaero ()
  (load "aero-interface.l")
  ;(load "/home/wesleyc/docs/catkin_ws/src/robot_model_compiler/euslisp/aero-stick-figure-interface.l")
  (aero-init)
;  (pprint (send *aero* :slots))
;  (print "")
;  (print "")
;  (print "")
;  (send *aero* :torso)
)

(testaero)
(objects *aero*)
